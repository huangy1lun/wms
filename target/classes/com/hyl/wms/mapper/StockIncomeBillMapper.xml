<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hyl.wms.mapper.StockIncomeBillMapper">
    <resultMap id="BaseResultMap" type="com.hyl.wms.domain.StockIncomeBill">
        <id column="id" property="id"/>
        <result column="sn" property="sn"/>
        <result column="vdate" property="vdate"/>
        <result column="status" property="status"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="total_number" property="totalNumber"/>
        <result column="audit_time" property="auditTime"/>
        <result column="input_time" property="inputTime"/>
        <association property="inputUser" javaType="com.hyl.wms.domain.Employee" columnPrefix="ie_">
            <id column="id" property="id"/>
            <result column="name" property="name"/>
        </association>
        <association property="auditUser" javaType="com.hyl.wms.domain.Employee" columnPrefix="ae_">
            <id column="id" property="id"/>
            <result column="name" property="name"/>
        </association>
        <association property="depot" javaType="com.hyl.wms.domain.Depot" columnPrefix="d_">
            <id column="id" property="id"/>
            <result column="name" property="name"/>
        </association>
        <collection property="items" ofType="com.hyl.wms.domain.StockIncomeBillItem" column="id"
                    select="com.hyl.wms.mapper.StockIncomeBillItemMapper.queryByBillId"/>
    </resultMap>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        DELETE FROM `stockincomebill`
        WHERE id = #{id}
    </delete>
    <insert id="insert" parameterType="com.hyl.wms.domain.StockIncomeBill" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `stockincomebill` (sn, vdate, status, total_amount, total_number, audit_time,
                                       input_time, input_user_id, auditor_id, depot_id)
        VALUES (#{sn}, #{vdate}, #{status}, #{totalAmount}, #{totalNumber}, #{auditTime},
                #{inputTime}, #{inputUser.id}, #{auditUser.id}, #{depot.id})
    </insert>
    <update id="updateByPrimaryKey" parameterType="com.hyl.wms.domain.StockIncomeBill">
        UPDATE `stockincomebill`
        SET sn           = #{sn},
            vdate        = #{vdate},
            total_amount = #{totalAmount},
            total_number = #{totalNumber},
            depot_id     = #{depot.id}
        WHERE id = #{id}
    </update>

    <update id="audit">
        UPDATE `stockincomebill`
        SET
            auditor_id = #{auditUser.id},
            audit_time = #{auditTime},
            status     = #{status}
        WHERE id = #{id}
    </update>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT sb.id, sb.sn, sb.vdate, sb.status, sb.total_amount, sb.total_number, sb.audit_time, sb.input_time,
            ie.id AS ie_id, ie.name AS ie_name,
            ae.id AS ae_id, ae.name AS ae_name,
            d.id AS d_id, d.name AS d_name
        FROM stockincomebill sb JOIN employee ie ON sb.input_user_id = ie.id
            JOIN depot d ON sb.depot_id = d.id
            LEFT JOIN employee ae ON sb.auditor_id = ae.id
        WHERE sb.id = #{id}
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
            id,
            sn,
            vdate,
            status,
            total_amount,
            total_number,
            audit_time,
            input_time,
            input_user_id,
            auditor_id,
            depot_id
        FROM `stockincomebill`
    </select>

    <sql id="where_sql">
        <where>
            <if test="beginDate != null">
                AND sb.vdate >= #{beginDate}
            </if>
            <if test="endDate != null">
                AND sb.vdate &lt;= #{beginDate}
            </if>
            <if test="depotId > -1">
                AND sb.depot_id = #{depotId}
            </if>
            <if test="status > -1">
                AND sb.status = #{status}
            </if>
        </where>
    </sql>

    <select id="queryForCount" resultType="int">
        SELECT COUNT(sb.id)
        FROM stockincomebill sb JOIN employee ie ON sb.input_user_id = ie.id
        JOIN depot d ON sb.depot_id = d.id
        LEFT JOIN employee ae ON sb.auditor_id = ae.id
        <include refid="where_sql"/>
    </select>

    <select id="queryForList" resultMap="BaseResultMap">
        SELECT sb.id, sb.sn, sb.vdate, sb.status, sb.total_amount, sb.total_number, sb.audit_time, sb.input_time,
        ie.id AS ie_id, ie.name AS ie_name,
        ae.id AS ae_id, ae.name AS ae_name,
        d.id AS d_id, d.name AS d_name
        FROM stockincomebill sb JOIN employee ie ON sb.input_user_id = ie.id
        JOIN depot d ON sb.depot_id = d.id
        LEFT JOIN employee ae ON sb.auditor_id = ae.id
        <include refid="where_sql"/>
        ORDER BY sb.id
        LIMIT #{startIndex},#{pageSize}
    </select>
</mapper>